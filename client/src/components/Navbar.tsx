import React from 'react';
import UserProfile from './UserProfile';

const Navbar = () => {
  return (
    <div className="text-blue">
      I am the beautiful Navbar
      <ul>
        {/* link to UserProfile */}
        {/* link to Neighborhood */}
        {/* link to Logout */}
        {/* link to Messages */}
      </ul>
    </div>
  );
};

export default Navbar;
